query PropertyBrandingList($filter: PropertyBrandingFilter) {
  response: propertyBrandingList(filter: $filter) {
    data {
      ... on PropertyBranding {
        key
        value
      }
    }
  }
}
