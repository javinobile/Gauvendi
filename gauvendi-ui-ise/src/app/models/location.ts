export interface ILocation {
  city: string;
  country: string;
  hostname: string;
  ip: string;
  loc: string;
  location: { lat: number, lng: number };
  org: string;
  postal: string;
  region: string;
  timezone: string;
}
